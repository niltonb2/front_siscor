// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  hmr: false,
  apiUrl: 'http://localhost:4000'
};

export const URL_API = 'https://apisiscor.grupomaqplan.com.br'
//export const URL_API = 'http://localhost:8000'
export const URL_VIACEP = `https://viacep.com.br/ws/`
export const URL_FRONT = 'http://localhost:4200'
export const API_KEY = 'key_5d0cd34bdbe56f206cdee891'

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
